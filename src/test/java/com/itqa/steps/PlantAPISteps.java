package com.itqa.steps;

/**
 * Step definitions for Plant Management API test cases (TC_PLA_API_01 – TC_PLA_API_10).
 * Tester: Pirapanchan R.
 *
 * HTTP steps (POST/GET/PUT/DELETE) are shared via CategoryAPISteps.
 * Sort assertion step is defined in SalesAPISteps to avoid duplication.
 */
public class PlantAPISteps {
    // All HTTP verb steps (POST/GET/PUT/DELETE) are defined in CategoryAPISteps (shared glue).
    // Plant-specific precondition steps (a plant exists, deletable plant, etc.) are
    // also defined in CategoryAPISteps.
    // No additional unique steps needed for Plant API scenarios.
}
